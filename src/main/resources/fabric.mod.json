{
  "schemaVersion": 1,
  "id": "log-that-block",
  "version": "${version}",
  "name": "Log that Block",
  "description": "Logs to the console whenever something happens to the blocks in your server.",
  "environment": "server",
  "depends": {
    "fabricloader": ">=0.4.8",
    "okyanus": ">=0.1.0"
  },
  "entrypoints": {
    "server": [
      "club.issizler.ltb.LogThatBlockMod::init"
    ]
  }
}
